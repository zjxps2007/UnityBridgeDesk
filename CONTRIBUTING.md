# 개발에 참여하기

변경하려는 동작과 재현 순서를 먼저 이슈나 PR에 적어 주세요. UI 문구는 한국어를 기본으로 사용합니다.

1. Windows에서 `global.json`에 지정된 .NET SDK와 PowerShell 7을 준비합니다.
2. `pwsh -File ./build/Verify.ps1 -Restore`로 빌드와 자동 검사를 수행합니다.
3. 화면 변경은 여러 창 크기에서 확인하고, 사용자 입력·선택·진행 중 작업을 유지하는지 확인합니다.
4. PR에는 변경 이유, 확인한 범위와 남은 제한을 적습니다.

자동 검사는 합성 자료를 사용합니다. Unity 프로젝트를 실제로 실행하는 진단과 유료 AI 호출은 기본 검사에 추가하지 마세요. 실행 기록이나 개인 인증 파일도 커밋하지 않습니다.

자세한 구조와 배포 방법은 [개발 가이드](docs/DEVELOPMENT.md)에 있습니다. 프로젝트는 [MIT 라이선스](LICENSE)를 사용합니다.
