# UnityBridge Desk 작업 지침

## UI·UX·디자인 작업

- UI를 설계하거나 수정할 때 먼저 [디자인 작업 기준](design/UI-DESIGN-WORKFLOW.md)을 읽고 따른다.
- 사용자는 생성형 AI가 반복적으로 만드는 전형적인 화면 구성을 피하고, 관련 디자인 스킬과 프로젝트 MD를 실제로 읽어 조합할 것을 요청했다. 참고 문서의 이름만 나열하거나 스킬 하나의 기본 스타일을 그대로 적용하지 않는다.
- 현재 사용자의 요청이 과거 문서보다 우선한다. 파스텔·연보라 정체성과 작은 구름 고양이를 유지하고, 벤치 정보와 조작을 우선한다.
- 작업 범위에 맞는 스킬·MD의 역할, 적용할 원칙과 제외할 원칙을 짧게 기록한다. 외부 SKILL.md를 읽은 것과 스킬 설치·검색 도구 실행을 구분한다.
- 실제 화면 검토로 결과를 확인한다. UI 변경으로 측정 구간·집계·성공 판정이 달라지지 않도록 한다.
- 사용자가 제안·브레인스토밍만 요청한 경우 구현하지 않는다. 이미 승인된 수정은 디자인 검토를 이유로 매번 재승인을 요구하지 않는다.

## 리틀 모델 엔진과 Desk의 분리

- 사용자 요청에 따라 바탕화면 독립 앱 이름은 **리틀 모델 엔진**, 실행 파일은 `LittleModelEngine.exe`이다. 리틀 모델 엔진 소스는 별도 저장소에서 관리하며 이 저장소에 포함하지 않는다.
- 이 앱의 `Characters/`·`Assets/`는 자체 파일이다. Desk 캐릭터 소스 링크, 빌드 시 복사, 설정 공유, Worker 감지나 실행 상태 동기화를 다시 추가하지 않는다.
- 이후 캐릭터 그림·크기·행동 변경은 요청한 앱에만 적용한다. 두 앱에 함께 적용하는 경우 사용자의 요청 범위를 확인하며 자동 전파하지 않는다.
